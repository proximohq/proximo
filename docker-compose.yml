version: '2'
services:
  database:
    image: mysql:latest
    env_file: .env
    environment:
      MYSQL_DATABASE: proximo
    ports:
      - "3306:3306"

  server:
    build: .
    volumes:
      - ./src:/usr/src/app/src
      - ./migrations:/usr/src/app/migrations
    ports:
      - 3000:3000
    command: sh /usr/src/app/start.sh
    links:
      - database
